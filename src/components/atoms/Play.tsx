import { Stream } from '../icons';

const Play = () => {
    return (
        <div className='bg-green-500  h-14 w-14 rounded-full flex items-center justify-center'>
            <Stream />
        </div>
    );
};

export default Play;
